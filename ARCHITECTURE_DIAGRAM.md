# Architecture & Deployment Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     RAILWAY DEPLOYMENT                           │
│                  (Public Internet - HTTPS)                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│  ┌──────────────────────┐  ┌──────────────────────────────┐     │
│  │ CDN / Browser        │  │  Load Balancer (Railway)      │     │
│  │ User Access          │  │  - SSL/TLS Termination       │     │
│  └──────────────────────┘  │  - Request Routing           │     │
│           ↓                └──────────────────────────────┘     │
│           └────────────────────────┬───────────────────────┘    │
│                                    │                             │
│    ┌───────────────────────────────┼───────────────────────┐   │
│    ↓                               ↓                       ↓    │
│ ┌─────────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐  │
│ │  Frontend   │  │ Backend  │  │ Detect   │  │  Stream     │  │
│ │             │  │          │  │ Model    │  │  Model      │  │
│ │ Vite Server │  │ Express  │  │ FastAPI  │  │ FastAPI     │  │
│ │             │  │ API      │  │          │  │             │  │
│ │ Port: 4173  │  │ Port:5000│  │ Port:8002│  │ Port: 8003  │  │
│ └─────────────┘  └──────────┘  └──────────┘  └─────────────┘  │
│      ↑               ↓              ↓              ↓            │
│      │      ┌────────┴────────┬─────────────┬────────┘         │
│      │      ↓                 ↓             ↓                   │
│      │    Model          Model File     Video Feed             │
│      └─── Detection      (best.pt)      Processing             │
│           API Call                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### 1️⃣ Frontend → Backend

```
User Browser (React)
    ↓
    ├─ POST /api/detect (image)
    └─ GET /api/status
        ↓
    Backend (Express)
        ├─ Validate request
        ├─ Forward to Detect Model
        └─ Return response
```

### 2️⃣ Frontend → Detect Model

```
User Browser
    ↓
    GET /detect (image upload)
        ↓
    Detect-Model (FastAPI)
        ├─ Load YOLO model
        ├─ Inference
        ├─ Extract keypoints
        └─ Return JSON
```

### 3️⃣ Frontend → Stream Model

```
User Browser
    ↓
    GET /video_feed (stream)
        ↓
    Stream-Model (FastAPI)
        ├─ Access camera
        ├─ Real-time processing
        ├─ Overlay detection
        └─ MJPEG stream
```

---

## 📊 Service Dependencies

```
FRONTEND
  ├─ calls → BACKEND
  ├─ calls → DETECT-MODEL
  └─ consumes → STREAM-MODEL

BACKEND
  ├─ proxies → DETECT-MODEL
  └─ proxies → STREAM-MODEL

DETECT-MODEL
  ├─ reads → model/best.pt
  └─ standalone service

STREAM-MODEL
  ├─ reads → model/best.pt
  ├─ accesses → webcam
  └─ independent service
```

---

## 🌐 Network Flow

```
Internet (HTTPS)
    ↓
Railway Router
    ├─ https://frontend-xyz.railway.app → [Frontend Port 4173]
    ├─ https://backend-xyz.railway.app → [Backend Port 5000]
    ├─ https://detect-model-xyz.railway.app → [Detect Port 8002]
    └─ https://stream-model-xyz.railway.app → [Stream Port 8003]
```

---

## 💾 Data Flow

```
User Upload Image
    ↓
Frontend (React)
    ├─ Convert to base64
    ├─ POST to Backend or Detect-Model
    ↓
Python Model
    ├─ Decode image
    ├─ YOLO inference
    ├─ Extract boxes, keypoints
    ↓
JSON Response
    ├─ Detection results
    ├─ Confidence scores
    └─ Keypoints coordinates
    ↓
Frontend Display
    ├─ Render bounding boxes
    ├─ Show results
    └─ Display confidence
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│        Railway Managed HTTPS             │
│  (Automatic SSL/TLS Certificates)        │
└─────────────────────────────────────────┘
              ↓
        ┌─────────────┐
        │  CORS Filter │
        └─────────────┘
              ↓
    ┌─────────────────────────┐
    │ Request Validation      │
    ├─ Content-Type check    │
    ├─ Size limit check      │
    └─ Rate limiting        │
              ↓
    ┌─────────────────────────┐
    │ Service Processing      │
    └─────────────────────────┘
```

---

## 📈 Scaling Architecture

```
High Traffic Scenario:

Frontend   →  Backend   →  Detect-Model
 (1x)        (1x)         (1x - CPU bound)
            ↓ loads
            ↓ distributed

Stream Model  (separate instance)
   (1x - independent video processing)

Scaling recommendations:
- Frontend: Stateless, easily scalable
- Backend: Stateless, can scale if proxying many requests
- Detect: CPU-bound, scale horizontally for concurrency
- Stream: I/O-bound, scale for multiple camera streams
```

---

## 🗂️ Database/Storage Architecture

```
Current setup (Stateless):
┌──────────────────────┐
│ Model Inference      │
│ - No database        │
│ - Model in memory    │
│ - Stateless responses│
└──────────────────────┘

Optional Future Additions:
┌──────────────────────┐
│ Database Layer       │
│ - PostgreSQL         │
│ - Store detection    │
│  history             │
│ - User management    │
└──────────────────────┘

┌──────────────────────┐
│ File Storage         │
│ - Railway Volumes    │
│ - Store models       │
│ - Cache results      │
└──────────────────────┘
```

---

## 🚦 Deployment Pipeline

```
┌────────────────────────────────────────────────┐
│         GitHub Repository                      │
│  (Push → Trigger Deployment)                  │
└────────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────────┐
│         Railway Webhook                        │
│  (Detect changes → Trigger build)             │
└────────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────────┐
│    Build Process (per service)                 │
│  1. Clone repo                                │
│  2. Install dependencies                      │
│  3. Build artifacts                           │
│  4. Run tests (optional)                      │
└────────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────────┐
│    Deploy Process                              │
│  1. Create container                          │
│  2. Set environment variables                 │
│  3. Start service                             │
│  4. Health check                              │
│  5. Route traffic                             │
└────────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────────────────┐
│    Running Services                            │
│  ✅ Frontend accessible                       │
│  ✅ Backend responsive                        │
│  ✅ Models running                            │
│  ✅ Monitoring active                         │
└────────────────────────────────────────────────┘
```

---

## 📡 CI/CD Flow

```
┌──────────────────┐
│ Developer        │
│ - git push       │
└──────────────────┘
         ↓
┌──────────────────┐
│ GitHub           │
│ - triggers hook  │
└──────────────────┘
         ↓
┌──────────────────────────────────────────┐
│ Railway Build (Parallel for all services)│
│                                           │
│ ┌──────────────┐  ┌──────────────┐     │
│ │ Frontend     │  │ Backend      │     │
│ │ npm install  │  │ npm install  │     │
│ │ npm run build│  │ (no build)   │     │
│ └──────────────┘  └──────────────┘     │
│                                           │
│ ┌──────────────┐  ┌──────────────┐     │
│ │ Detect-Model │  │ Stream-Model │     │
│ │ pip install  │  │ pip install  │     │
│ └──────────────┘  └──────────────┘     │
└──────────────────────────────────────────┘
         ↓
┌──────────────────┐
│ Railway Deploy   │
│ (All services)   │
└──────────────────┘
         ↓
┌──────────────────┐
│ Running          │
│ ✅ All services  │
└──────────────────┘
```

---

## 🎯 Component Communication Matrix

```
        │ Frontend │ Backend │ Detect │ Stream │
────────┼──────────┼─────────┼────────┼────────┤
Frontend│    -     │   ✅    │  ✅    │  ✅   │
────────┼──────────┼─────────┼────────┼────────┤
Backend │    ✅    │    -    │  ✅    │  ✅   │
────────┼──────────┼─────────┼────────┼────────┤
Detect  │    ✅    │   ✅    │   -    │   ✅  │
────────┼──────────┼─────────┼────────┼────────┤
Stream  │    ✅    │   ✅    │   ✅   │   -   │
────────┼──────────┼─────────┼────────┼────────┤

Legend:
✅ = Can communicate (via API calls)
-  = Same service
```

---

## 📋 Technology Stack

```
Frontend Layer
├─ React (UI)
├─ Vite (Build tool)
├─ Tailwind CSS (Styling)
└─ Axios (HTTP client)

Backend Layer
├─ Node.js 18
├─ Express (Web framework)
├─ CORS (Cross-origin)
└─ Environment variables

Model Layer (Python)
├─ Python 3.11
├─ FastAPI (Web framework)
├─ Uvicorn (ASGI server)
├─ PyTorch (Deep learning)
├─ Ultralytics YOLO (Object detection)
├─ OpenCV (Computer vision)
└─ NumPy (Numerical computing)

Infrastructure
├─ Railway (Platform)
├─ GitHub (Code repository)
├─ Docker (Containerization)
├─ NGINX (Reverse proxy - Railway managed)
└─ SSL/TLS (Encryption - Railway managed)
```

---

## 🔌 Port Allocation

```
Public URLs (HTTPS)
├─ frontend-xyz.railway.app (443/HTTPS)
├─ backend-xyz.railway.app (443/HTTPS)
├─ detect-model-xyz.railway.app (443/HTTPS)
└─ stream-model-xyz.railway.app (443/HTTPS)
           ↓
Railway Reverse Proxy (internal)
           ↓
Internal Ports
├─ Frontend: 4173
├─ Backend: 5000
├─ Detect Model: 8002
└─ Stream Model: 8003
```

---

**Last Updated:** December 28, 2025

**For deployment guide, see:** RAILWAY_DEPLOYMENT_GUIDE.md

**For quick start, see:** RAILWAY_QUICK_START.md
